# Ranges Page QA

## Status

- `/ranges/` exists.
- Header navigation links to `/ranges/`.
- Products page links to `/ranges/`.
- Footer links to `/ranges/` as "All ranges".
- Bamboo is not visible on the ranges page.
- Range cards are grouped as a full browsing page rather than being dumped into `/products/`.

## Notes

- The ranges page currently contains a large range set, which is appropriate as the full range index.
- Keep `/products/` simplified and use `/ranges/` for deeper browsing.

## Remaining Manual Review

- Review exact colour counts and product specs where supplier data was incomplete.
